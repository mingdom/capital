# Override ignores to include the data directory for Codex
# The data/ folder is gitignored, but we want Codex to see it.
!data/
!data/**

# Ensure private performance data remains visible to Codex
!data/private/
!data/private/**
